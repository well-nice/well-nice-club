
  # Finalize Cursor app design

  This is a code bundle for Finalize Cursor app design. The original project is available at https://www.figma.com/design/CsdGaz5VvIvNn21sQXqrK0/Finalize-Cursor-app-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  